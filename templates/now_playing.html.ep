% layout 'main', title => 'Now Playing', hide_nav => 1, load_js => ['now_playing.js'];
<div id="now_playing" class="container-fluid text-center" style="color: white; text-shadow: black 0 0 0.3em;">
  <h3 v-if="now_playing">{{ now_playing.artist }} - {{ now_playing.title || now_playing.raw_request }}</h3>
</div>
